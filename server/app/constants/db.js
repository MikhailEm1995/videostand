const DB_HOST = '';
const DB_NAME = '';
const DB_USERNAME = '';
const DB_PASSWORD = '';

const TABLES = {
    USERS: 'users',
    SUBSCRIPTIONS: 'subscriptions',
    VIDEOSTANDS: 'videostands',
    MEDIA: 'media'
};

module.exports = {
  DB_HOST, DB_NAME, DB_USERNAME, DB_PASSWORD,
  TABLES
};
